# `web-search` (feature id: `web-search`)

Tavily-backed `web_search` tool. Ops switch: `ENABLED_FEATURES=...,web-search`.

Registers on `PluginRegistry` with skill group `knowledge`. Env: `TAVILY_API_KEY`.
