# `update-agent-memory`

Prisma-only `update_agent_memory` tool. Always loaded with `AiOrchestrationModule` (no separate feature id).

Writes `AgentMemory` rows scoped to conversation or user. Skill group: `knowledge`.
